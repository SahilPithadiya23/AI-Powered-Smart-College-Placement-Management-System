{
  "name": "smart-college-placement-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Enterprise-level MERN SaaS for college placement management.",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace backend",
    "lint": "npm run lint --workspaces --if-present"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
